---
description: 笔记写完后的审查清单（第二轮检查）
alwaysApply: true
---

# 笔记审查清单

写完 notes 文件后，必须执行以下审查流程。**审查是独立的第二轮**，不在写作过程中同时进行。

---

## 触发条件

当 Agent 完成一个 notes 文件的写作（新建或大幅修改）后，必须立即进入审查流程。

**审查范围**：一个领域的**所有 notes 文件都必须审查，包括 `01-overview.md`**，以及**领域 `README.md`**。不得跳过任何文件。批量写作时，最终审查必须覆盖全部文件。

---

## 审查流程

### Step 0：README 审查

README 是导航性质，审查内容与 notes 不同：

1. **学习路径表一致性**：README 中的学习路径表列出的所有文件是否都已创建？文件名、主题描述是否与实际 notes 内容一致？
2. **知识维度一致性**：README 中的知识维度表是否与 `01-overview.md` 一致？不能有矛盾
3. **推荐资源有效性**：链接格式是否正确（不需要逐一打开验证，但 URL 格式不能明显有误）
4. **背景与起点准确性**：是否反映用户的真实学习背景和起点

README 不需要检查术语依赖、公式、检测问题等 notes 专属项。

### Step 1：术语依赖检查

逐段扫描本章内容，列出所有**非日常用语的术语和概念**。对每个术语：

1. 是否在**本章前文**已经定义/解释过？
2. 如果没有，是否在**前序章节**（按学习路径）中讲解过？
3. 如果都没有 → **必须修复**：在本章首次使用处补充解释，或在前序章节补充

**常见遗漏**：
- 训练机制类：mini-batch、batch size、epoch、learning rate schedule
- 数学类：Hessian 矩阵、特征值、凸函数
- 工程类：GPU、显存、并行计算

### Step 2：公式完整性检查

对每个公式：

1. **每个符号**是否有解释？（在公式前后或符号表中）
2. 符号含义是否和前文一致？（同一个字母在不同章节不要表示不同的东西）
3. 是否给了**具体数字例子**建立直觉？（至少关键公式要有）

### Step 3：解释方向检查

对每个涉及"A 比 B 大/小/多/少"的陈述：

1. 是否给了**具体数值对比**？
2. 方向是否正确？（特别注意用户容易搞反的地方）

### Step 4：结构检查

1. 文件开头是否有元信息区（主维度、关键关系）？
2. 是否有 `### 公式速查卡`（理工科章节）？
3. 检测问题是否包含至少一道计算题？
4. 计算题是否有 `> 提示：用 xxx 公式`？
5. 检测问题区是否只有题目和空白的"你的回答："？

### Step 5：前后章节一致性

1. 本章的前置知识列表是否准确？用户是否真的学过这些？
2. 本章是否引用了后续章节才会讲的概念？（不允许前向引用未解释的概念）
3. 与学习路径表（README.md）中的描述是否一致？

---

## 审查输出

审查完成后，在对话中简要报告：

```
📋 审查完成：
- 术语依赖：✅ 通过 / ⚠️ 发现 N 个未定义术语（已修复）
- 公式完整性：✅ / ⚠️
- 方向检查：✅ / ⚠️
- 结构检查：✅ / ⚠️
- 前后一致性：✅ / ⚠️
```

如果有修复，列出修复内容。
